# Propuesta: Home discovery — Fase 8.1 (ajustes agendamiento)

## Why
Separar el agendamiento inteligente (comparador IA) del agendamiento dirigido (proveedor + servicio desde categorías/explorar) y alinear el home con demanda por modelo.

## What Changes
- Rail «Más elegidos para tu modelo» reemplaza ofertas (ofertas comentadas para fase futura).
- `HomeAgendamientoRapidoButton` reemplaza barra de búsqueda.
- Flag `agendamientoInteligente` en navegación y `FormularioSolicitud`.
- Perfil proveedor: agendar por servicio con proveedor preseleccionado.
- Categorías: carrusel horizontal con nombre completo.
- Banner contextual: solo CTA Agendar (sin «Ver salud»).

## Non-goals
- Comparador IA dentro del sheet del home (sigue en formulario tras ubicación/fecha).

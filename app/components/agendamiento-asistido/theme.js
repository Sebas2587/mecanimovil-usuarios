import { COLORS } from '../../design-system/tokens/colors';

/** Tokens usados por el asistente (primary es escala numérica, no `.main`). */
export const AGENDAMIENTO_THEME = {
  primary: COLORS.primary[500],
  primaryLight: COLORS.primary[50],
  primaryMuted: COLORS.primary[100],
  warningLight: COLORS.warning.light,
  warningDark: COLORS.warning.dark,
  textPrimary: COLORS.text.primary,
  textSecondary: COLORS.text.secondary,
  textDisabled: COLORS.text.disabled,
  borderLight: COLORS.border.light,
  backgroundPaper: COLORS.background.paper,
  backgroundSubtle: COLORS.neutral.gray[100],
};
